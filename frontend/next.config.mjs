/** @type {import('next').NextConfig} */
const nextConfig = {
  // Output static export for Vercel (optional — remove if using SSR/API routes later).
  // output: 'export',
}

export default nextConfig
